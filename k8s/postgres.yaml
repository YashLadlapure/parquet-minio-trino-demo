apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: hive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:14
          env:
            - name: POSTGRES_USER
              value: hive
            - name: POSTGRES_PASSWORD
              value: hive
            - name: POSTGRES_DB
              value: metastore
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: hive
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
